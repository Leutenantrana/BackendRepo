{
    "name": "fsopen_backend",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "cross-env NODE_ENV=test node --test",
        "start": "cross-env NODE_ENV=production node index.js",
        "dev": "cross-env NODE_ENV=development nodemon index.js",
        "build:ui": "rm -rf build && cd ../FullstackFrontend/ && npm run build && cp -r build ../backendRepo"
    },
    "author": "sagar rana",
    "license": "ISC",
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^16.4.5",
        "express": "^4.19.2",
        "gitignore": "^0.7.0",
        "mongodb": "^6.8.0",
        "mongoose": "^8.4.4"
    },
    "devDependencies": {
        "cross-env": "^7.0.3",
        "nodemon": "^3.1.4",
        "supertest": "^7.0.0"
    }
}